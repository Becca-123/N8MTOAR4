projection;
strict ( 2 );
use draft;

define behavior for ZZ1_PV_WOTYPE //alias <alias_name>
use etag
{
  use create;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}